import { create } from 'zustand'

// MÓDULO 03: AUTENTICACIÓN CON ZUSTAND
// ==========================================
// TODO: 1. Crear un store de Zustand para controlar el estado de inicio de sesión.
// TODO: 2. Definir un estado inicial `isLoggedIn` (booleano) inicializado en false.
// TODO: 3. Crear una acción `login` que establezca `isLoggedIn` en true.
// TODO: 4. Crear una acción `logout` que establezca `isLoggedIn` en false.

export const useAuthStore = create((set) => ({
  // ¡Define tu estado y acciones aquí!
}))