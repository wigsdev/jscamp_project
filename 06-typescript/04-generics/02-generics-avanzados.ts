// MÓDULO 06: GENERICS AVANZADOS
// ==========================================
// TODO: 1. Crear una interfaz genérica `ApiResponse<T>` que contenga campos como `data: T`, `status: number` y `success: boolean`.
// TODO: 2. Crear una función genérica que acepte una restricción (constraint) usando la palabra clave `extends` (ej. `function logLength<T extends { length: number }>(arg: T): void`).
// TODO: 3. Probar la función pasando un string, un array y un objeto con propiedad `length`.

// ¡Escribe tus ejemplos aquí!
