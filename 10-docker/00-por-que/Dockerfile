# MÓDULO 10: DOCKER - DOCKERFILE BÁSICO
# ==========================================
# TODO: 1. Elegir la imagen base oficial de Node.js (ej. `node:22-alpine` para una imagen liviana).
# TODO: 2. Establecer el directorio de trabajo dentro del contenedor (ej. `/app`).
# TODO: 3. Copiar el archivo `app.js` al directorio de trabajo en el contenedor.
# TODO: 4. Definir el comando por defecto para iniciar la aplicación (`node app.js`).

# ¡Comienza tu Dockerfile aquí!
